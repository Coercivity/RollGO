export enum LocalizationNamespace {
  COMMON = 'common',
  NAVBAR = 'navbar',
  AUTH = 'auth',
  MOVIE = 'movie',
  LOBBY = 'lobby',
  WHEEL = 'wheel',
  USER_SETTINGS = 'userSettings',
  VALIDATIONS = 'validations',
  MAIN_PAGE = 'main',
}
