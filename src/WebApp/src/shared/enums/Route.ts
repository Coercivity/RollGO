export enum Route {
  ROOT = '/',
  PROFILE = '/profile',
  LOBBY = '/lobby',
  REGISTRATION = '/registration',
  LOGIN = '/login',
  RULES = '/terms-of-use',
  USER_SETTINGS = '/user-settings',
}
